spring:
  datasource:
    postgresql:
      hrdb:
        driverClassName: org.postgresql.Driver
        jdbcUrl: jdbc:postgresql://localhost:5432/hrdb
        username: postgres
        password: '123456'

mybatis:
  configuration:
    postgresql:
      hrdb:
        map-underscore-to-camel-case: on